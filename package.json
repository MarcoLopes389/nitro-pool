{
  "name": "nitro-pool",
  "version": "1.1.1",
  "description": "Run dynamic functions in parallel using process pools and worker threads — no worker files, no boilerplate, just execution.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib && rimraf *.tsbuildinfo",
    "build": "npm run clean && tsc",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MarcoLopes389/nitro-pool.git"
  },
  "bugs": {
    "url": "https://github.com/MarcoLopes389/nitro-pool/issues"
  },
  "homepage": "https://github.com/MarcoLopes389/nitro-pool",
  "keywords": [
    "threads",
    "pool",
    "child_process",
    "worker_threads"
  ],
  "files": [
    "lib/**/*"
  ],
  "author": "Marco Lopes",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.2"
  }
}
